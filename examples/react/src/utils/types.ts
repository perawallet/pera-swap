/**
 * Type definitions for the CustomUIExample component
 */

/**
 * Form state interface for the swap configuration
 */
export interface FormState {
  assetInId: string
  assetOutId: string
  amount: string
  slippage: string
  swapperAddress: string
  depositAddress: string
}
